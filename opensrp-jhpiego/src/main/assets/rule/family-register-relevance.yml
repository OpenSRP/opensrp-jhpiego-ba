---
name: step2_surname
description: surname relevance
priority: 1
condition: "!step2_same_as_fam_name.contains('same_as_fam_name')"
actions:
 - "isRelevant = true"
---
name: step2_dob_entered
description: dob_entered relevance
priority: 1
condition: "!step2_dob_unknown.contains('dob_unknown')"
actions:
  - "isRelevant = true"

---
name: step2_age
description: age relevance
priority: 1
condition: "step2_dob_unknown.contains('dob_unknown')"
actions:
  - "isRelevant = true"

---
name: step2_preg_1yr
description: preg_1yr calculation
priority: 1
condition: "(step2_sex.contains('Female') && ((step2_age != '') && (step2_age >= 15 && step2_age <= 49))) || (step2_sex.contains('Female') && ((step2_dob_entered != '') && (helper.formatDate(step2_dob_entered, 'y') >= 15 && helper.formatDate(step2_dob_entered, 'y') <= 49)))"
actions:
  - "isRelevant = true"

---
name: step2_leader_other
description: leader_other relevance
priority: 1
condition: "step2_leader.contains('Other')"
actions:
- "isRelevant = true"

